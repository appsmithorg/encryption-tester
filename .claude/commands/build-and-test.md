Run the full Maven build lifecycle for this project.

Steps:
1. Run `mvn clean package` to compile and package
2. Report any compilation errors or warnings
3. If tests exist, report test results
4. Confirm the output JAR was produced at `target/encryption-tool-0.0.1-SNAPSHOT.jar`
